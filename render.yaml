services:
  - type: web
    runtime: static
    name: dds-stories
    buildCommand: npm ci && npm run ladle:build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 20

